<Window x:Class="Gui.TestWindowForStretchAndScrollbars"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TestWindowForStretchAndScrollbars" Height="300" Width="300">
        <DockPanel>
        <Grid DockPanel.Dock="Top" Background="Azure">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Name="FileName" Text="no file" Grid.Column="0"/>
            <TextBox Name="FileChangeEvent" Text="no event" Grid.Column="1" HorizontalContentAlignment="Right"/>
                    </Grid>
            <TextBox Background="Beige"/>
        </DockPanel>
</Window>
